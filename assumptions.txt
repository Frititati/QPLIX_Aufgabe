Assumption 1:
	- The CSV file is formatted always correctly, always Delimited with ";", always follows the same type throught the file, files are always ordered by date
Assumption 2:
	- Whenever an Estate is bought there is always a Building transaction as well, on the same date, and vice versa.